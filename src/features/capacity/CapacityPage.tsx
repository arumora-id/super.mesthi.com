import { PageHeader, Card, Badge } from '../../components/ui'
export function CapacityPage(){return <><PageHeader title="Capacity" description="Capacity enforcement remains server-side. This screen is prepared for the dedicated platform capacity/admin endpoint planned in C4.5/C4.6."/><Card><div className="flex items-center justify-between"><div><div className="font-medium">Current runtime capacity</div><p className="mt-2 text-sm text-slate-500">C4 currently enforces the authoritative runtime limit in the backend. A global admin read endpoint is required before this panel can safely show live aggregate values.</p></div><Badge tone="amber">Backend endpoint pending</Badge></div></Card></>}
